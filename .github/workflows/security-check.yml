name: Security check

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  sensitive-files:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Reject tracked secrets and local databases
        run: backend/scripts/check-sensitive-files
